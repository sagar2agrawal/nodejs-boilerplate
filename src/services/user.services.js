/* eslint-disable import/extensions */
import User from '../models/user.models.js';

// export const listAllUsers = async (userListDTO) => {
//   const { page, size } = userListDTO;

//   let result = await User.find();

// };
